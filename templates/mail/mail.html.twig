<!DOCTYPE html>
<html>
<head>
    <title>Achat des tickets</title>
    <style>
        {{ include('mail/css/mail.css') }}
    </style>
</head>
<body>
    <header class="col-12">
        <img src="https://www.digitourist.com/images/logos/98899881.jpg" alt="musé du louvre" width="200" height="200">
    </header>
    <div class="container">
        {% if order is defined and order is not empty %}
            <div class="recap">

                <h1>Récapitulatif de votre commande</h1>

                Le total de votre commande est de <strong>{{ order.total }} € TTC</strong>.<br>
                Votre code de commande est <strong>{{ order.stripeToken }}</strong>.<br>
                Vous pouvez récupérer vos tickets à cette adresse
                <a href="{{ url('recap',{'stripeToken':order.stripeToken}) }}">{{ url('recap',{'stripeToken':order.stripeToken}) }}</a>

                <table class="table text-center">
                    <thead>
                        <tr>
                            <th scope="col">{{ 'recap.ticketNumber'|trans }}</th>
                            <th scope="col" class="visible-md">{{ 'recap.fare'|trans }}</th>
                            <th scope="col">{{ 'recap.price'|trans }}</th>
                            <th scope="col">{{ 'recap.firstname'|trans }}</th>
                            <th scope="col">{{ 'recap.lastname'|trans }}</th>
                            <th scope="col">{{ 'recap.country'|trans }}</th>
                            <th scope="col">{{ 'recap.birthday'|trans }}</th>
                        </tr>
                    </thead>
                    {% for ticket in order.ticket %}
                        <tbody>
                        <tr>
                            <th scope="row">{{ loop.index }}</th>
                            <td>{{ ticket.fare.name }}</td>
                            <td>{{ ticket.fare.price }}</td>
                            <td>{{ ticket.visitor.firstName }}</td>
                            <td>{{ ticket.visitor.lastName }}</td>
                            <td>{{ ticket.visitor.country }}</td>
                            <td>{{ ticket.visitor.birthday.date|date('d/M/Y') }}</td>
                        </tr>
                        </tbody>
                    {% endfor %}
                </table>
            </div>
        {% endif %}
    </div>
</body>
</html>

