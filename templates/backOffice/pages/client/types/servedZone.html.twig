{% extends 'backOffice/pages/client/edit.html.twig' %}

{% block title %}{{ c.legalInformation.corporateName ?? 'nouveau' }} - Zones desservies {% endblock %}

{% block header %}
    {{ parent() }}
    {{ include('backOffice/base/parts/header.html.twig',
        {"breadcrumb":[
            [path('_admin_client_index'), "Clients"],
            [ '#' , c.legalInformation.corporateName ?? 'nouveau'],
            [path('_admin_client_edit_about',{"clientId":c.id}), "Zones desservies"]
        ]}) }}
{% endblock %}

{% block form %}
    <h2>Zones desservies</h2>
    {{ form_start(form) }}
    <div class="form-group checkboxCascade">
        {{ render(controller('App\\Controller\\Front\\RenderController::getCheckboxCascade',
            {'entities': servedZone, 'form': form.children.servedZone.children})) }}
    </div>
    <div class="text-right">
        <button type="submit" class="btn btn-success ">Soumettre</button>
    </div>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('build/js/script-min.js') }}"></script>
{% endblock %}


