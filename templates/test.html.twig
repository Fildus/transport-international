{% extends 'base.html.twig' %}

{% block title %}tests{% endblock %}

{% block body %}
    {% if form is defined %}
        {{ form_start(form) }}
        <div class="checkboxCascade">
            <ul>
                {% include 'form.html.twig' with {'form': form.children.activity.children, 'structure': structureActivities, 'lvl': 0, 'transTree': 'activitiesForm'} %}
            </ul>
        </div>
        <hr>
        <div class="checkboxCascade">
            <ul>
                {% include 'form.html.twig' with {'form': form.children.servedZone.children, 'structure': structureServedZone, 'lvl': 0,'transTree': 'servedZone'} %}
            </ul>
        </div>
        <input type="submit" value="Envoyer">
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
